# AutumnAUTools

## 绩点与目标成绩计算器

React 版首页快捷入口或悬浮球导航进入，路由为 `/#/gpa`。

- 按课程学分加权计算 GPA，支持直接录入绩点或自定义百分制分段换算；百分制初始规则只是示例。
- P / N / EX 自动排除；非空课程编号相同的记录取最高绩点；其他不计入课程手动取消勾选。
- 使用课程明细，或直接填写已有 GPA 与学分，反推剩余课程所需平均绩点，提示不可达目标。
- 按平时成绩、期末占比反推所需期末分数，提供整数分门槛。
- 自动保存到本机浏览器，支持导出文字结果。清空、删除和示例替换支持撤销。

计算口径参考[本科生院 2026-07-16 说明](https://jwc.cau.edu.cn/art/2026/7/16/art_41139_1122721.html)，课程归属、替代和排名口径需按个人情况核对。

验证：在 `app/` 下运行 `pnpm test:gpa`（Node.js 22.6+，使用内置 TypeScript 类型剥离）和 `pnpm build`。

## DeepSeek 学习助手

React 版路由为 `/#/deepseek`，采用纯前端 BYOK 模式：用户填写自己的 DeepSeek API 地址、Key 和模型，浏览器直接请求接口，调用费用由用户账户承担。默认地址为 `https://api.deepseek.com`，默认模型为 `deepseek-chat`；接口需要允许浏览器跨域访问。

API Key 默认保存在当前浏览器会话，用户主动勾选后才保存到本机。实现只使用项目既有的 React、React Router、Vite 和浏览器 Fetch API，没有引入额外聊天 SDK。具体开发记录和开源工具清单见 [DEVELOPMENT_LOG.md](DEVELOPMENT_LOG.md)。

## 排课表工具
v 1.0.0
初始化GUI，提供班级课表调用、自助排课模式、自由选课模式三种模式；
<br>
v 1.1.0
移除自由选课模式；调整GUI；
<br>
v 1.1.1
修复已知bug；优化操作逻辑；
<br>
v 1.1.2
修复已知bug；调整GUI；
<br>
v 1.2.0
修复已知bug；新增课程通知单悬浮窗功能；新增时间偏好的引导逻辑；新增本地缓存逻辑，重启页面时可继承上次操作状态；
<br>
v 1.2.3
修复已知bug；
<br>
## 题库
v 1.0.0
初始化，设置目录区域；
<br>

## 许可证

本项目采用 [Apache License 2.0](LICENSE) 开源许可证。
